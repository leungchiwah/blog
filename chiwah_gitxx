git config --global user.name 'lzh'
git config --global user.email 'lzh@qq.com'
git config --global color.ui true
git config --list
mkdir /chiwah_gitdata
cd /chiwah_gitdata
ll -a   
..git
git init
touch chiwah_git.txt
git status

git add chiwah_git.txt
git commit -m "add newfile github"

or

git commit -am "add newfile github"
